@default = "assertCssValue"
definition {

	function assertCssValue(locator2, locator1) {
		selenium.waitForElementPresent(${locator1}, ${value1});

		selenium.assertCssValue(${locator1}, ${value1}, ${locator2});
	}

}