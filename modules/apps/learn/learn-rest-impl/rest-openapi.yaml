info:
    description:
        "A Java client JAR is available for use with the group ID 'com.liferay', artifact ID
        'com.liferay.portal.learn.rest.client', and version '1.0.4'.. A Java client JAR is available for use with the
        group ID 'com.liferay', artifact ID 'com.liferay.learn.rest.client', and version '1.0.0'."
    license:
        name: "Apache 2.0"
        url: "http://www.apache.org/licenses/LICENSE-2.0.html"
    title: "Learn"
    version: v1.0
openapi: 3.0.1
paths:
    "/messages/{resource}.json":
        get:
            parameters:
                - in: path
                  name: resource
                  required: true
                  schema:
                      type: string
            responses:
                200:
                    content:
                        application/octet-stream:
                            schema:
                                format: binary
                                type: string
            tags: ["Messages"]