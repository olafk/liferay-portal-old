##
## Test Suites
##

    modified.files.includes[relevant][headless-delivery-rule]=**

    relevant.rule.names=\
        headless-delivery-rule

    test.batch.names[relevant][headless-delivery-rule]=\
        functional-tomcat90-mysql57-jdk8

    #
    # Portal Hotfix Release
    #

    test.batch.run.property.query[functional-bundle-tomcat-mysql57-jdk8][test-portal-hotfix-release]=\
        ${test.batch.run.property.query[functional-tomcat*-mysql*-jdk8][relevant][headless-delivery-rule]}

    #
    # Relevant
    #

    test.batch.run.property.query[functional-tomcat*-mysql*-jdk8][relevant][headless-delivery-rule]=\
        (portal.acceptance == true) AND \
        (testray.main.component.name ~ "Content Management Headless")

##
## Testray
##

testray.main.component.name=Content Management Headless